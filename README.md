# calculator_fastapi
A simple server-side calculator built using FastAPI, HTML, and CSS with no JavaScript. Each button sends a request to the backend, which processes the arithmetic and updates the display. Clean UI, easy to understand, and ideal for beginners learning FastAPI and server-driven web apps.
